// Màn hình dành cho Thứ cấp
import 'package:flutter/material.dart';

class SubDealerScreen extends StatelessWidget {
  const SubDealerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("Sub Dealer Screen"));
  }
}
